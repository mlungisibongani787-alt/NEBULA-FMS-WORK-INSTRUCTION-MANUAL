import {I} from '../lib/icons.js';import {store} from '../core/store.js';import {go} from '../core/router.js';import {esc} from '../lib/utils.js';import {bridgeStatus,connectBridge} from '../core/bridge.js';
export const nav=[['dashboard','Dashboard','home'],['receiving','Receiving','receiving'],['dispatch','Dispatch','dispatch'],['cycle-count','Cycle Count','count'],['inventory','Inventory','inventory'],['locate','Locate (Geiger)','locate'],['audit','Audit','audit'],['investigations','Investigations','investigate'],['reports','Reports','reports'],['master-data','Master Data','data'],['devices','Devices','devices'],['settings','Settings','settings']];
export function shell(page){const st=bridgeStatus();return `<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="brand-mark">RF</div><div><b>RFID SYSTEM</b><span>OPERATIONS CONTROL</span></div></div><nav>${nav.map(n=>`<button class="nav ${page===n[0]?'active':''}" data-route="${n[0]}"><span class="nav-icon">${I[n[2]]}</span><span>${n[1]}</span></button>`).join('')}</nav><div class="site"><span>●</span><span>${esc(store.get().site)}</span></div></aside><main class="main"><header class="topbar"><div class="search">${I.search}<input id="global-search" aria-label="Global search" placeholder="Search SKU, EPC, LPN, Location, PO..."></div><div class="top-actions"><button class="bridge-status ${st.state}" id="header-bridge" title="Open Devices">● ${st.state==='online'?'Reader Bridge Online':st.state==='error'?'Bridge Error':'Reader Offline'}</button><span class="bell">${I.bell}</span><div class="user"><b>BN</b><span><strong>Inventory Controller</strong><small>${esc(store.get().site)}</small></span></div></div></header><div id="content" class="content"></div></main></div>`}
export function panel(title,body,action=''){return `<section class="panel"><div class="panel-head"><h3>${title}</h3>${action}</div>${body}</section>`}
export function button(label,cls='secondary',attrs=''){return `<button class="${cls}" ${attrs}>${label}</button>`}
export function pageTitle(title,desc,badge=''){return `<div class="page-title"><div><h1>${title}</h1><p>${desc}</p></div>${badge?`<span class="status-chip">${badge}</span>`:''}</div>`}
export function bindLayout(){document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>go(b.dataset.route));document.querySelector('#header-bridge')?.addEventListener('click',()=>go('devices'));document.querySelector('#global-search')?.addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target.value.trim()){localStorage.setItem('rfid-last-search',e.target.value.trim());go('inventory')}})}
