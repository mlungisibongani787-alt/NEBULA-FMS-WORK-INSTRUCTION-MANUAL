import {esc,fmt} from '../lib/utils.js';export function toast(msg,error=false){document.querySelector('.toast')?.remove();const d=document.createElement('div');d.className='toast'+(error?' error':'');d.textContent=msg;document.body.appendChild(d);setTimeout(()=>d.remove(),3200)}
export function modal(title,body){document.querySelector('.modal-backdrop')?.remove();const d=document.createElement('div');d.className='modal-backdrop';d.innerHTML=`<div class="modal"><div class="modal-head"><h2>${esc(title)}</h2><button class="secondary" data-close-modal>Close</button></div><div class="modal-body">${body}</div></div>`;document.body.appendChild(d);d.addEventListener('click',e=>{if(e.target===d||e.target.closest('[data-close-modal]'))d.remove()});return d}
export function statCards(cards){return `<div class="stats">${cards.map(c=>`<div class="stat ${c.cls||''}"><b>${c.icon||''}</b><strong>${fmt(c.value)}</strong><span>${esc(c.label)}</span><small>${esc(c.sub||'')}</small></div>`).join('')}</div>`}
