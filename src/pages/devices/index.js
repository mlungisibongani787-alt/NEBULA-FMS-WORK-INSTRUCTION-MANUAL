import {pageTitle,panel} from '../../components/layout.js';import {store} from '../../core/store.js';import {bridgeStatus,connectBridge,disconnectBridge,bridgeCommand} from '../../core/bridge.js';import {toast} from '../../components/ui.js';import {esc} from '../../lib/utils.js';
export function render(){const s=store.get(),st=bridgeStatus();return `<div class="page">${pageTitle('Devices & RFID Readers','Production reader management for the Zebra RFD8500 through the Android Zebra RFID SDK.','RFD8500')}<div class="grid-two">${panel('Reader Bridge',`<div class="device-card"><div class="device-symbol">◉</div><div><h3>Android RFID Bridge</h3><p>Connects the web system to the Zebra RFD8500 over the supported Zebra SDK path.</p></div><span class="connected ${st.state==='online'?'':'off'}"><i></i>${esc(st.state)}</span></div><label style="margin-top:12px">Bridge WebSocket URL<input id="bridge-url" value="${esc(s.settings.bridgeUrl)}"></label><div class="button-row"><button class="primary" id="bridge-connect">Connect Bridge</button><button class="secondary" id="bridge-disconnect">Disconnect</button></div><div id="bridge-result" class="test-result ${st.state==='online'?'ok':'bad'}">${esc(st.message)}</div>`)}${panel('RFD8500 Controls',`<div class="button-row"><button class="primary" id="reader-inventory">Start RFID Inventory</button><button class="secondary" id="reader-stop">Stop Inventory</button><button class="secondary" id="reader-readers">Refresh Readers</button></div><label>Reader power (dBm × 100) <input id="reader-power" type="range" min="0" max="3000" step="100" value="${s.settings.readerPower}"><small style="color:#7f91a5" id="power-label"></small></label><label>Beeper volume<select id="reader-beep"><option>LOW</option><option selected>MEDIUM</option><option>HIGH</option><option>QUIET</option></select></label><div class="notice"><p>The RFD8500 supports Bluetooth RFID through Zebra's SDK. If the reader is in batch mode, the SDK must handle the batch state before normal operations.</p></div>`)}</div>${panel('Connected Readers',`<div id="reader-list">${s.devices.readers.length?s.devices.readers.map(r=>`<div class="reader"><span>◉</span><div><b>${esc(r.name)}</b><small>${esc(r.model||'RFD8500')}</small></div><span class="connected ${r.connected?'':'off'}"><i></i>${r.connected?'Connected':'Offline'}</span></div>`).join(''):`<div class="empty">No reader has reported through the bridge yet.</div>`}</div>`)}</div>`}
export function bind(){const s=store.get();document.querySelector('#bridge-connect').onclick=async()=>{const url=document.querySelector('#bridge-url').value.trim();store.set({settings:{...s.settings,bridgeUrl:url}});const ok=await connectBridge(url);document.querySelector('#bridge-result').className='test-result '+(ok?'ok':'bad');document.querySelector('#bridge-result').textContent=ok?'Bridge connected. Waiting for RFD8500 status.':'Bridge connection failed. Start the bridge server and check the URL.'};document.querySelector('#bridge-disconnect').onclick=()=>{disconnectBridge();toast('Bridge disconnected')};document.querySelector('#reader-inventory').onclick=()=>{try{bridgeCommand('inventoryStart');toast('RFID inventory started')}catch(e){toast(e.message,true)}};document.querySelector('#reader-stop').onclick=()=>{try{bridgeCommand('inventoryStop');toast('RFID inventory stopped')}catch(e){toast(e.message,true)}};document.querySelector('#reader-readers').onclick=()=>{try{bridgeCommand('listReaders');toast('Reader refresh requested')}catch(e){toast(e.message,true)}};const powerInput=document.querySelector('#reader-power');const powerLabel=document.querySelector('#power-label');const showPower=()=>{if(powerLabel)powerLabel.textContent=` ${(+powerInput.value/100).toFixed(2)} dBm`};powerInput.oninput=showPower;powerInput.onchange=e=>{store.set({settings:{...store.get().settings,readerPower:+e.target.value}});try{bridgeCommand('setPower',{power:+e.target.value})}catch{}};showPower();document.querySelector('#reader-beep').onchange=e=>{store.set({settings:{...store.get().settings,beeper:e.target.value}});try{bridgeCommand('setBeeper',{level:e.target.value})}catch{}}}
