import {pageTitle,panel} from '../../components/layout.js';import {store} from '../../core/store.js';import {esc} from '../../lib/utils.js';import {modal,toast} from '../../components/ui.js';
export function render(){const rows=store.get().investigations;return `<div class="page">${pageTitle('Investigations','Resolve missing, extra, duplicate, wrong-location and quantity-variance exceptions.')}<div class="button-row"><button class="primary" id="new-case">Create Investigation</button></div>${panel(`Open Cases (${rows.length})`,rows.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>Case</th><th>Type</th><th>EPC/SKU</th><th>Location</th><th>Status</th><th>Created</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(x.id)}</td><td>${esc(x.type)}</td><td>${esc(x.reference)}</td><td>${esc(x.location)}</td><td><span class="pill warn">${esc(x.status)}</span></td><td>${new Date(x.created).toLocaleString()}</td></tr>`).join('')}</tbody></table></div>`:`<div class="empty">No investigation cases.</div>`)}</div>`}
export function bind(){document.querySelector('#new-case').onclick=()=>modal('Create Investigation',`<div class="form-grid"><label>Type<select id="case-type"><option>Missing RFID</option><option>Extra RFID</option><option>Wrong Location</option><option>Duplicate EPC</option><option>Quantity Variance</option></select></label><label>Reference<input id="case-ref" placeholder="EPC / SKU / LPN"></label><label>Location<input id="case-location"></label><label>Notes<textarea id="case-notes"></textarea></label></div><button class="primary" id="save-case">Create Case</button>`);document.addEventListener('click',e=>{if(e.target.id==='save-case'){const s=store.get();const c={id:`INV-${Date.now()}`,type:document.querySelector('#case-type').value,reference:document.querySelector('#case-ref').value,location:document.querySelector('#case-location').value,notes:document.querySelector('#case-notes').value,status:'OPEN',created:new Date().toISOString()};store.addInvestigation(c);store.addEvent('INVESTIGATION',`${c.id} created`);document.querySelector('.modal-backdrop')?.remove();toast('Investigation created');setTimeout(()=>location.reload(),150)}})}
