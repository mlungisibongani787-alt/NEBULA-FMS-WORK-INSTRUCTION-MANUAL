import {pageTitle,panel} from '../../components/layout.js';import {store} from '../../core/store.js';import {esc,fmt} from '../../lib/utils.js';
export function render(){const s=store.get();return `<div class="page">${pageTitle('RFID Audit','Audit a location, LPN, tote or carton against the expected item-level RFID ledger.')}<div class="grid-two">${panel('Audit Target',`<div class="form-grid"><label>Target type<select id="audit-type"><option>Location</option><option>LPN</option><option>SKU</option><option>ASN</option></select></label><label>Target value<input id="audit-value" placeholder="e.g. STAGE or LPN0000001619"></label></div><button class="primary" id="run-audit">Run RFID Audit</button><div id="audit-summary" style="margin-top:10px"></div>`)}${panel('Audit Rules',`<div class="switch"><span>Detect missing RFID</span><b class="pill ok">ON</b></div><div class="switch"><span>Detect extra RFID</span><b class="pill ok">ON</b></div><div class="switch"><span>Detect duplicate EPC</span><b class="pill ok">ON</b></div><div class="switch"><span>Record reader/session evidence</span><b class="pill ok">ON</b></div>`)}</div>${panel('Audit Detail',`<div id="audit-table" class="empty">Run an audit to view discrepancies.</div>`)}</div>`}
export function bind(){document.querySelector('#run-audit').onclick=()=>{const s=store.get(),type=document.querySelector('#audit-type').value,val=document.querySelector('#audit-value').value.trim();if(!val)return;const inv=s.inventory.filter(x=>{if(type==='Location')return x.location===val;if(type==='LPN')return x.lpn===val;if(type==='SKU')return x.sku===val;if(type==='ASN')return x.asn===val});const expected=s.asnLines.filter(x=>{if(type==='Location')return x.location===val;if(type==='LPN')return x.lpn===val;if(type==='SKU')return x.sku===val;if(type==='ASN')return x.asn===val}).reduce((a,x)=>a+x.expected,0);const found=inv.length,variance=found-expected;document.querySelector('#audit-summary').innerHTML=`<div class="count-kpis"><div class="mini-kpi"><span>Expected</span><b>${fmt(expected)}</b></div><div class="mini-kpi"><span>RFID Found</span><b>${fmt(found)}</b></div><div class="mini-kpi"><span>Variance</span><b>${variance}</b></div><div class="mini-kpi"><span>Result</span><b>${variance===0?'MATCH':variance<0?'SHORT':'EXTRA'}</b></div></div>`;document.querySelector('#audit-table').innerHTML=inv.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>EPC</th><th>SKU</th><th>LPN</th><th>Location</th><th>Status</th><th>Last Seen</th></tr></thead><tbody>${inv.map(x=>`<tr><td>${esc(x.epc)}</td><td>${esc(x.sku)}</td><td>${esc(x.lpn)}</td><td>${esc(x.location)}</td><td>${esc(x.status)}</td><td>${x.lastSeen?new Date(x.lastSeen).toLocaleString():'—'}</td></tr>`).join('')}</tbody></table></div>`:`<div class="empty">No RFID tags found for this target.</div>`;store.addEvent('AUDIT',`RFID audit run — ${type}: ${val}`,{variance})}}
